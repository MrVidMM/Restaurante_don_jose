import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Restaurante_don_jose',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
